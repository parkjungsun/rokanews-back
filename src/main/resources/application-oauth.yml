spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            scope:
            redirect-uri:
          naver:
            client-id:
            client-secret:
            client-authentication-method:
            authorization-grant-type:
            scope:
            client-name:
            redirect-uri:
          kakao:
            client-id:
            client-secret:
            client-authentication-method:
            authorization-grant-type:
            scope:
            client-name:
            redirect-uri:
        provider:
          naver:
            authorization-uri:
            token-uri:
            user-info-uri:
            user-name-attribute:
          kakao:
            authorization-uri:
            token-uri:
            user-info-uri:
            user-name-attribute:
app:
  oauth2:
    authorized-redirect-uris: